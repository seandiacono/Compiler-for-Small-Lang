ff Average(toAverage[]:float, count:int):float{
    let total[3]:float = {1.0, 2.3 4.5};

    for(let i:int = 0; i<count; i = i+1){
        total[i] = total[i-1] + toAverage[i];
    }

    return total/count;
}